// Given a string, return a version without the first and last char, so "Hello"
// yields "ell". The string length will be at least 2.
// withoutEnd("Hello") → "ell"
// withoutEnd("java") → "av"
// withoutEnd("coding") → "odin


var a = "scope"
var b = "magnet"
var c = "posted"
var d = "supercalifragleistic"

console.log(a.substr(1,a.length-2))
console.log(b.substr(1,b.length-2))
console.log(c.substr(1,c.length-2))
console.log(d.substr(1,d.length-2))